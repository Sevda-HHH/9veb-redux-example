import { Counter } from '../../components/Counter';
import { TextList } from '../../components/TextList';

export const Home = () => {
    return (<>
        <main>
            <Counter />
            <TextList />
        </main>
    </>
    )
}